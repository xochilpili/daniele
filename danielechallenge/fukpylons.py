#err_mail=''
        #if not mail:
        #       err_mail="Please enter an email addres"
        #       return render('/derived/page/simpleform.html', extra_vars={'err_msg' : err_mail})
        #elif '@' not in mail:
        #       err_mail="Please enter a valid email."
        #       return render("/derived/page/simpleform.html", extra_var={'err_msg' : err_mail})
        #else:
        #       dns = mail.split('@')[1]
        #       if '.' not in dns:
        #               return "Missing domain in Email Address"
        #       if not dns.split('.')[-1]:
        #               return "Please, this is not a valid Email Address"
                #if err_mail:
                #       return render('/derived/page/simpleform.html')
                #else:


